# faraGamer
Install wrapper for Microsoft's fara LLM on a gamer-spec PC (&lt;32Gb vram) using WSL.  Assumes Windows 11.  Hosts LLM locally using llamma.cpp instead of vllm and attempts to use your own chrome browser instead of edge.
